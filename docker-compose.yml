version: "3.9"

networks:
  rag_network:
    driver: bridge

services:
  ollama:
    container_name: ollama
    build:
      context: .
      dockerfile: DockerfileLLM
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: ["gpu"]
        limits:
          cpus: "2.0"
          memory: 8g
    environment:
      - CUDA_VISIBLE_DEVICES=0
      - OLLAMA_MAX_GPU_MEM=14GB
      - OLLAMA_ORIGINS=http://rag-service:5000
    volumes:
      - ollama-models:/home/app/.ollama/models
    ports:
      - "11434:11434"
    restart: on-failure
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 30s
      timeout: 5s
      retries: 3
    security_opt:
      - no-new-privileges:true
#      - seccomp:docker-default
#      - apparmor:docker-default
    cap_drop:
      - ALL
    stop_grace_period: 30s
    networks:
      - rag_network

  qdrant:
    container_name: qdrant
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage
    restart: on-failure
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6333/ready"]
      interval: 30s
      timeout: 5s
      retries: 3
    security_opt:
      - no-new-privileges:true
#      - seccomp:docker-default
#      - apparmor:docker-default
    cap_drop:
      - ALL
    stop_grace_period: 30s
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 4g
    networks:
      - rag_network

  rag-service:
    container_name: rag-service
    build:
      context: .
      dockerfile: DockerfileRAG
    depends_on:
      - ollama
      - qdrant
    environment:
      - OLLAMA_URL=http://ollama:11434
      - QDRANT_URL=http://qdrant:6333
    ports:
      - "5000:5000"
    volumes:
      - rag-data:/app/data
      - rag-output:/app/output
    restart: on-failure
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    security_opt:
      - no-new-privileges:true
#      - seccomp:docker-default
#      - apparmor:docker-default
    cap_drop:
      - ALL
    stop_grace_period: 30s
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2g
    networks:
      - rag_network

volumes:
  ollama-models:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /mnt/data/rag/ollama/models

  rag-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /mnt/data/rag/data

  rag-output:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /mnt/data/rag/output

  qdrant_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /mnt/data/rag/qdrant
